# Privileged Operator Session

This session tracks QSECOFR administration on CLAIMS400. Mutations write audit journal entries, job log lines, and state changes for `DSPEVDDIFF` and `GENRPT TYPE(*PRIV)`.

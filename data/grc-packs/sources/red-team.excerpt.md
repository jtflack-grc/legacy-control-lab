# Can You Red Team an IBM i?

find where the platform is intentionally delegating authority too broadly

excessive special authorities, dangerous *PUBLIC rights, owner-adopted programs

authority-model failures and integration failures

IBM documents host-server ports for ACS-supported services, states that Telnet can autostart by default

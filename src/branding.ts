/** Public product name shown in the lab shell, 5250 screens, and CLI output. */
export const APP_NAME = "Legacy Control Lab";

/** Runtime engine subtitle (not the product name). */
export const RUNTIME_NAME = "IBM i Evidence Runtime";

/** One-line public subtitle. */
export const APP_SUBTITLE = "An IBM i-style governance and security range";

/** Primary marketing tagline. */
export const TAGLINE = "Control evidence, from the green screen out.";

/** Short positioning for social / GitHub description. */
export const POSITIONING_SHORT =
  "A local IBM i-style governance and security range where auditors review evidence and QSECOFR shows what privileged access really means.";
